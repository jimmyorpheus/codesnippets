# MySQL Testo

Create a database:
```sql
CREATE DATABASE database_name; 
```

Delete a database:
```sql
DROP DATABASE [IF EXISTS] database_name;
```


Get Data from table:

```sql
SELECT *
FROM tablename
WHERE id = 5;
```
